<?php
/**
 * JK위드미 백엔드 설정 파일 (예시)
 * ---------------------------------------------------------------
 *  실제 사용 시 이 파일을 복사해서 "config.php" 로 만들고,
 *  호스팅에서 발급받은 DB 정보로 값을 채우세요.
 *      cp config.sample.php config.php
 *  config.php 는 절대 외부에 공개되면 안 됩니다.
 * ---------------------------------------------------------------
 */

return [
    // --- 데이터베이스 접속 정보 (호스팅사에서 발급) ---
    'db' => [
        'host'    => 'localhost',   // 보통 localhost. 호스팅에 따라 다름
        'port'    => 3306,
        'name'    => 'jkwithme',     // DB 이름
        'user'    => 'DB_USER',      // DB 아이디
        'pass'    => 'DB_PASSWORD',  // DB 비밀번호
        'charset' => 'utf8mb4',
    ],

    // --- 사이트 기본 설정 ---
    'site' => [
        // 문의 접수 완료 후 이동할 페이지 (사이트 루트 기준 경로 또는 전체 URL)
        'redirect_after_submit' => '/',
        // 관리자 페이지 로그인 세션 유지 시간(초). 기본 3시간
        'session_lifetime' => 10800,
    ],
];

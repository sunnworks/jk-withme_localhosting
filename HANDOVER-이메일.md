# JK위드미 홈페이지 복원 소스코드 — 전달 안내 (이메일 양식)

> 재단/병원 담당자께 압축파일과 함께 보내는 **인수인계용 이메일 예시**입니다.
> 그대로 복사해 사용하시거나, 상황에 맞게 문구만 다듬어 보내시면 됩니다.

---

**제목: JK위드미 홈페이지 복원 소스코드 전달 (호스팅용 / 로컬테스트용 2종)**

안녕하세요,

현재 운영 중인 JK위드미 홈페이지를 **공개된 화면·이미지·영상을 그대로 참고해
최대한 동일하게 재구성(복원)한 소스코드**를 전달드립니다. 첨부는 압축파일 2개입니다.

## ■ 배경 — 왜 '복원본'인가
현재 홈페이지는 개발사(하룹)에서 제작했으나 **소스코드를 넘겨받지 못한 상황**입니다.
그래서 공개된 화면을 기반으로 화면·이미지·영상을 복사해 동일하게 재현하고,
**상담문의 접수·관리자 기능·이메일 알림은 새로 개발해 붙였습니다.**

즉 이 결과물은 "완벽하게 검증된 정식 운영본"이라기보다,
**원본을 최대한 되살린 복원본(약 80~90% 완성도)** 입니다. 지금 목적은 하나입니다 —
**훗날 하룹에서 끝내 소스를 못 받더라도, 이 복원본을 출발점으로 새 개발사에 이관하면
홈페이지를 거의 그대로 되살려 운영·유지보수할 수 있게 하는 것**입니다.
일종의 '보험'이라고 보시면 됩니다.

## ■ 성격과 한계 (미리 안내드립니다)
- 원본과 **100% 동일하진 않을 수 있습니다.** (지도·일부 폰트·외부 연동 등은 실제 도메인에서만 완전 동작)
- 일부 기능은 미세하게 다르거나 다듬어야 할 수 있습니다. **최종 마무리 테스트와 잔여 버그 수정은,
  이 소스를 넘겨받는 개발사가 진행**하는 것을 전제로 만들었습니다. 그 상태에서 정식 홈페이지로 살리고,
  이후 유지보수도 가능합니다.
- 반대로 화면 구성과 **핵심 기능(상담접수·관리자·이메일알림·검색·전후사진 필터)은 원본과 거의 동일**하게 동작합니다.

## ■ 구조 — 하나의 웹서버(PHP)로 동작
정적 화면과 동적기능·백엔드를 **하나의 웹서버(PHP)에서 함께 동작**하도록 추가 개발했습니다.
별도 서버 없이 일반 웹호스팅 하나로 운영되는, 완결적으로 동작하는 구조입니다.

## ■ 원본 대비 정리·개선한 부분
- 하룹(개발사)의 흔적(폴더/파일/클래스명 등)을 JK위드미 기준으로 정리
- **마케팅·애널리틱스·광고 추적 코드(방문분석·리타겟팅 등) 전면 제거** → 외부로 방문자 데이터가 새지 않음
- 사이트 검색, 전후사진 실시간 필터 등 동적기능을 자체 백엔드로 복원
- 이미지·영상 캐싱/압축 적용으로 로딩 속도 최적화

## ■ 첨부파일 2개
1. **jk-withme** — 실제 웹호스팅 배포용 (MySQL 기반)
2. **jk-withme_localhosting** — 로컬(내 PC) 테스트용 (PHP만 있으면 실행, 설치 간편)

두 폴더 모두 안에 **`docs` 폴더(설계·인수인계 문서 6종)** 가 들어 있어, 나중에 유지보수를 맡길
개발사에 그대로 넘기면 구조·DB·배포·보안을 바로 파악할 수 있습니다. (`docs/00-인수인계-개요.md` 부터 순서대로)

## ■ 내 PC에서 바로 보는 방법 (jk-withme_localhosting)
1. 압축 해제
2. 윈도우: `run-local.bat` 더블클릭 / 맥: 터미널에서 `bash run-local.sh`
   (PHP가 없으면 먼저 설치 — 폴더 안 `README-LOCAL.md` 참고)
3. 브라우저에서 열기
   - 홈페이지: http://localhost:8000
   - 관리자: http://localhost:8000/backend/admin/ (아이디 `admin` / 비밀번호 `jkwithme!2026`)
4. 홈에서 상담신청 폼 작성 → 관리자 목록에 뜨는지 확인

> ※ 회원가입/로그인은 별도 기능이 없어 숨김(hidden) 처리했습니다.
> ※ 로컬에서는 이메일이 실제 발송되진 않습니다(접수·조회는 정상). 실제 발송은 웹호스팅 + SMTP 설정에서 동작합니다.

## ■ 실제 운영 (웹호스팅)
Yes24 등 일반 웹호스팅(PHP+MySQL 지원)에 올리면 월 몇만원 수준으로 운영 가능합니다.
배포 절차는 `INSTALL.md`에 단계별로 정리되어 있습니다.

> ※ **배포 후 관리자 기본 비밀번호는 반드시 변경**해 주세요.

## ■ 보안
- SQL인젝션·XSS 방지, 스팸/과다요청 차단, 관리자 로그인 보호 등 기본 보안 적용
- 개인정보 보호와 공격 대비를 위해 운영 시 **HTTPS(SSL) 적용을 권장** (자세한 내용은 `docs/05-보안.md`)

궁금한 점 있으면 언제든 연락 주세요.
감사합니다.
